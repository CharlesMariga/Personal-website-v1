import React from "react";
import styled from "styled-components";

const Thanks = () => {
  return <Wrapper>Thanks</Wrapper>;
};

const Wrapper = styled.div`
  height: 100vh;
  display: flex;
  flex-direction: column;
  justify-content: center;
`;

export default Thanks;
